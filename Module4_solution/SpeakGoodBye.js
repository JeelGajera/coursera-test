(function (window) {

	var byeSpeaker = {}; 
	byeSpeaker.speakWord = "GoodBye";
	byeSpeaker.bye = function () {
	consol.log(byeSpeaker.speakWord + " " + names[i]);
	}

	window.byeSpeaker = byeSpeaker;

}) (window);